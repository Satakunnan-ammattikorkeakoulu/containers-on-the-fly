meta {
  name: Get Availability Timeline
  type: http
  seq: 6
}

get {
  url: {{root_url}}/reservation/get_availability_timeline?startDate=2030-02-01T00:00:00&endDate=2030-02-07T00:00:00
  body: none
  auth: bearer
}

auth:bearer {
  token: {{user_token}}
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });

  test("Response is successful", function() {
    expect(res.body).to.have.property('status');
    expect(res.body.status).to.equal(true);
  });

  test("Response has data", function() {
    expect(res.body).to.have.property('data');
  });
}
